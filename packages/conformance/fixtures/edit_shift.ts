/** Stacked logical units for Tier I edit-shift assertions (see golden.test.ts). */
export function top(): number {
  return 0;
}

export function mid(): number {
  return 1;
}

export function bottom(): number {
  return 2;
}
