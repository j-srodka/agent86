/** Three stacked units — baseline mangles middle unit with a leading `export` in the splice (see conformance edit-shift). */
export function top(): number {
  return 0;
}

export function mid(): number {
  return 1;
}

export function bottom(): number {
  return 2;
}
