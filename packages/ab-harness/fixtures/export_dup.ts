/** Second function is edited; baseline supplies `export` inside `new_text` and duplicates `export` (parse_error). */
export function keep(): number {
  return 0;
}

export function victim(): number {
  return 1;
}
