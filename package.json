{
  "name": "agent86",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "build:mcp": "pnpm --filter @agent86/mcp-server run build",
    "test": "pnpm --filter ts-adapter run build && pnpm --filter @agent86/mcp-server run build && pnpm -r run test",
    "ab:bench": "pnpm --filter ab-harness start",
    "ab:bench:expanded": "pnpm --filter ab-harness start -- --profile expanded"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
